# Base da API. Em desenvolvimento, deixe /api para usar o proxy do Vite
# (vite.config.ts redireciona /api → http://127.0.0.1:8000).
# Em produção, use a URL pública da API, por exemplo:
# VITE_API_URL=https://api.exemplo.com
VITE_API_URL=/api
